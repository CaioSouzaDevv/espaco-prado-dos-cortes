
version: '3.8'

services:
  postgres:
    image: postgres:18-alpine
    container_name: prado_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: prado
    ports:
      - '5433:5432'
    volumes:
      - ./postgres-data:/var/lib/postgresql
